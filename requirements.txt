# Streamlit app runtime dependencies (Streamlit Community Cloud reads this file).
# Versions pinned to the environment the .joblib models were trained in.
streamlit==1.40.2
scikit-learn==1.6.1
pandas==2.2.3
numpy==1.26.4
joblib==1.4.2

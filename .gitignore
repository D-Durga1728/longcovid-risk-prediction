# ── Python ──────────────────────────────────────────────────────────────────
__pycache__/
.pytest_cache/
*.pyc
*.pyo
.venv/
venv/

# ── Logs / scratch run output ───────────────────────────────────────────────
*.log
run_out.txt
app_boot.log
analysis_run.log
covid_analysis.log

# ── Large model artifacts NOT needed by the deployed app ────────────────────
# The app only loads imputer.joblib, scaler.joblib and model_primary_deployed.joblib.
# These others are big (Stacking 121MB exceeds GitHub's 100MB limit) and unused at runtime.
analysis_output/models/model_Stacking.joblib
analysis_output/models/model_RF.joblib
analysis_output/models/model_GB.joblib
analysis_output/models/model_XGBoost.joblib
analysis_output/models/model_LR.joblib

# ── Local editor / agent settings ───────────────────────────────────────────
.claude/

# ── Large analysis CSVs NOT needed by the deployed app ──────────────────────
# The app reads only analysis_output/advanced/*.csv and the embedded PNGs.
# These raw/cleaned cohort dumps are large and unused at runtime.
analysis_output/core_data/

# ── Internal docs (planning/handoff/notes) — keep private, not for public repo ─
REPORT_NOTES.md
REPORT_HANDOFF.md
MASTER_REPORT_PACKAGE.md
STREAMLIT_HANDOFF.md
POWERBI_BUILD_GUIDE.md
POWERBI_DASHBOARD_SPEC.md

# ── Offline analysis pipeline — not needed by the deployed app ───────────────
covid_analysis_full.py
advanced_methods.py
test_analysis.py
requirements-analysis.txt

# ── OS noise ────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

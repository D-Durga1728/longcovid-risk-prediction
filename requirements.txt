# Streamlit app runtime dependencies (Streamlit Community Cloud reads this file).
# Versions pinned to the environment the .joblib models were trained and serialized in,
# so imputer.joblib / scaler.joblib / model_primary_deployed.joblib load without warnings.
streamlit==1.40.2
scikit-learn==1.8.0
pandas==2.3.3
numpy==2.4.6
joblib==1.5.3

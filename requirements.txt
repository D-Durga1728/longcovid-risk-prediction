# Streamlit app runtime dependencies (Streamlit Community Cloud reads this file).
# Minimum versions match the environment the .joblib models were trained in.
streamlit>=1.40.0
scikit-learn>=1.5.0
pandas>=2.0.0
numpy>=1.24.0
joblib>=1.3.0
